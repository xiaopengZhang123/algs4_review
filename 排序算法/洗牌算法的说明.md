# 洗牌算法
这里有n张牌,洗牌算法是每次生成1-i的随机数,然后在这些随机数里面进行交换
```
for(int i=0;i<n;i++){
    int r=(0-i)之间的随机数
    swap(arr,i,r);
    //arr是卡牌数组
}

```

而不是下面这样的
```
for(int i=0;i<n;i++){
    int r=(0-n)之间的随机数
    swap(arr,i,r);
}

```